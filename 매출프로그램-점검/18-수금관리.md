# 18. 마감관리 > 수금관리 — 테스트 결과

- 웹: `/closing/collection` (전용 화면 `features/collection/`)
- 요구사항정의서: **23p** — 「기존이관-**무수정**」
- 레거시: `수금관리.vb` (20.7KB) → `AmtData`
- 판정: 🔴 **요구 항목 절반 이상 미구현. 문서는 「완료」로 기재돼 있음**

---

## ① ★ `DB-27` 확정 — 매출건 FK 가 없습니다

`AmtData` 컬럼 실측 (`ConvDB.vb`):
```
collDate(수금일자) · collSeq · writeDate(기장일자) · custCode · custName
· collKind(수금구분) · collType(입금구분) · collAmt(금액)
· prmNum(어음번호) · prmExpiryDate(만기일자) · prmBankName(은행명) · prmBranchName(지점명)
· memo(비고)
```
**13컬럼, 매출건 FK(`salesData.idx` 등) 없음** — 거래처 코드만 있습니다.

요구사항 `DB-27` 주장이 확정됐습니다:
> receipts=AmtData, **매출건 FK가 없어 거래처 단위 러닝밸런스만 가능**.
> 매출건별 매칭 관리가 필요하다면 신규 설계 필요

---

## ② 🔴 요구 데이터 항목 10종 중 **3종만** 구현

요구 23p 「주요 데이터 항목」:
> 수금일자, **기장일자**, 거래처명, **수금구분**, **입금구분**, 금액,
> **어음번호**, **만기일자**, **은행/지점명**, 비고

웹 실측 — 그리드 5컬럼 · 등록 폼 5필드:
```
그리드: 수금일 | 거래처명 | 수금액 | 수단 | 비고
등록폼: 수금일 · 거래처 · 수단 · 수금액 · 비고
```

| 요구 항목 | 레거시(AmtData) | 웹 | |
|---|---|---|---|
| 수금일자 | `collDate` | ✅ | |
| **기장일자** | `writeDate` | **없음** | 🔴 |
| 거래처명 | `custName` | ✅ | |
| **수금구분** | `collKind` | ⚠️ `수단` 하나로 합침 | |
| **입금구분** | `collType` | ⚠️ 위와 같음 | |
| 금액 | `collAmt` | ✅ | |
| **어음번호** | `prmNum` | **없음** | 🔴 |
| **만기일자** | `prmExpiryDate` | **없음** | 🔴 |
| **은행/지점명** | `prmBankName`·`prmBranchName` | **없음** | 🔴 |
| 비고 | `memo` | ✅ | |

**3/10 완전 구현.** 수금구분·입금구분은 2축인데 웹은 `수단`(현금/계좌이체/카드/어음/상계) 1축으로 합쳤습니다.

### ⚠️ 내부 모순 — 「어음」을 고를 수 있는데 어음 정보를 못 넣습니다

웹 `수단` 선택지에 **`어음`이 있습니다.** 그런데 어음번호·만기일자·은행/지점명 입력란이
없습니다. 어음으로 수금하면 **어느 어음인지 기록할 방법이 없습니다.**

레거시는 이 4개 컬럼(`prmNum`·`prmExpiryDate`·`prmBankName`·`prmBranchName`)을
**어음 수금 전용**으로 두고 있습니다.

---

## ③ 🔴 조회대상기준 3종 미구현

요구 23p:
> 조회대상기준 **3종(기장일자 / 수금일자 / 거래처(수금일기준))**

레거시 실측 (`수금관리.vb:80~86`) — 라디오 3개:
```vb
If RadioButton_OrderCollect.Checked      ' 수금일자 기준
ElseIf RadioButton_OrderWrite.Checked    ' 기장일자 기준  (기본값, 38행)
ElseIf RadioButton_Cust.Checked          ' 거래처 기준
```

**웹에는 없습니다.** 조회 기준 선택 자체가 없습니다.

---

## ④ 🔴 일계(일자별 소계) 미구현

요구 23p:
> **일자별 소계(일계) 자동 계산**

요구사항이 *"11페이지 제품수불부와 유사한 계층적 집계 패턴"* 이라고 연결해 둔 항목입니다.
웹 화면에서 `일 계`·`소 계` 문자열이 **검출되지 않았습니다.**

> 11번에서 확인했듯 `subtotals.ts` 가 그룹 소계를 지원하고, 이번에 바깥 계층까지
> 추가했으므로 **기반은 이미 있습니다.** 수금일 기준 그룹만 얹으면 됩니다.

---

## ⑤ 🔴 CRUD 중 C·R 만 — 수정·삭제 없음

요구 23p:
> 입금 내역의 **등록/수정/삭제/조회**. **CRUD 전체 가능 화면으로 권한 정책 필요**

- 웹 버튼: **`수금 등록`** 하나뿐
- `features/collection/api.ts` 에 `update`·`delete` **없음**

**권한 제어는 정상 동작합니다** — `viewer` 로 로그인하면 [수금 등록]이 비활성됩니다 ✅

---

## ⑥ ⚠️ 문서는 「완료」로 기재돼 있습니다

| 문서 | 기재 |
|---|---|
| `개발현황-보고-260731.md:95` | 23 수금관리 · 기존이관-무수정 · **완료** · *"거래처 선택 시 미수 잔액을 함께 표시. 과수금은 선수금 가능성이 있어 차단하지 않고 경고만 표시"* |
| `레거시-대비-구현현황.md:71` | 🔷 전용 구현 · *"미수 잔액 힌트, 과수금 경고(차단 아님)"* |

**추가 기능(미수 잔액 힌트·과수금 경고)은 좋은 개선**입니다. 다만 **요구 항목의 절반 이상이
빠진 상태에서 「완료」로 기재**돼 있어, 이 항목을 보고 판단하면 실제와 다릅니다.

> `레거시-대비-구현현황.md:71` 은 페이지 번호도 **25p 로 잘못 적혀** 있습니다(수금관리는 23p).

---

## ⑦ 작동 테스트

| # | 항목 | 결과 |
|---|---|---|
| C1 | 조회대상기준 3종 | **없음** 🔴 |
| C2 | 요구 데이터 항목 10종 | **3/10** 🔴 |
| C3 | 일계 자동 계산 | **없음** 🔴 |
| C4 | CRUD 버튼(등록/수정/삭제) | **등록만** 🔴 |
| C5 | viewer 권한 — 등록 비활성 | **PASS** ✅ |
| — | 콘솔 오류 | **0건** |

---

## 수정 후보 (신규)

1. 🔴 **어음 4필드**(어음번호·만기일자·은행명·지점명) — `수단=어음` 을 고를 수 있는데
   기록할 수 없는 **내부 모순** 상태
2. 🔴 **기장일자** 컬럼 — 요구·레거시 모두 있음
3. 🔴 **조회대상기준 3종** 라디오 — 레거시 실측 확인됨
4. 🔴 **일계(일자별 소계)** — `subtotals.ts` 기반 이미 있음
5. 🔴 **수정·삭제** — 요구가 "CRUD 전체"로 명시
6. ⚠️ 수금구분·입금구분 2축 분리 — 현재 `수단` 1축

## 문서 정정 후보 (신규)

- `개발현황-보고-260731.md:95` — 수금관리 **「완료」 재검토** (요구 10종 중 3종 구현)
- `레거시-대비-구현현황.md:71` — 페이지 번호 **25p → 23p** 오기
