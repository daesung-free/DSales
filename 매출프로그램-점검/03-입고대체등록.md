# 03. 주문/출고관리 > 입고/대체 등록 — 테스트 결과

- 웹: `/orders/inbound` (전용 화면)
- 요구사항정의서: **8p**(일반입고/단순이고) + **9p**(세트해체·조립 BOM)
- 레거시: `UC_TabPages.vb` 의 입고/대체 탭 → `invenData` 테이블
- 판정: 구조 ✅ / **요구 필드 3종 누락 🔴**

---

## ① 레거시 대비 — 문서 주장 전부 확정

`invenData` INSERT 컬럼(주석 원문, UC_TabPages.vb:707):
```
inputDate, tradeDate, tradeClass, tradeType, tradeSeq, custCode, custName,
catCode, catName, bookCode, bookName, bookSeq, bookCnt, part, memo
```
**15컬럼. 수량(`bookCnt`)만 있고 금액·단가·원가가 없습니다.**

grep 전수 결과:

| 개념 | 레거시 | 근거 |
|---|---|---|
| 원가 / 입고단가 | **0건** | `원가|unitCost|inCost|입고단가` grep 전무 |
| 이고(창고 간 이동) | **0건** | `이고` grep 전무 |
| BOM / 조립 / 해체 | **0건** | `조립|해체|BOM` grep 전무 |
| 창고 | **0건** | (02번에서 확인) |
| 위탁 | **0건** | (01번에서 확인) |

→ 요구사항정의서의 `BE-20`(원가 부재)·`BE-21`(이고 없음)·`BE-22`(위탁 자동이고 없음)·
   `BE-23`(BOM 없음) 주장이 **전부 실측으로 확정**됐습니다.

**이 화면은 '일반입고'를 빼면 전량 신규 개발이 맞습니다.** 그리고 일반입고조차
원가가 없어 순매출·이익률 산출의 전제가 성립하지 않았습니다.

---

## ② 요구사항 대비 — 🔴 필드 3종 누락

웹 실측 폼 필드(`InboundScreen.tsx` 라벨 전수):
```
입고일 · 창고 · 분류명 · 도서명 · 수량 · 비고
이동일 · 출발 창고 · 도착 창고 · 분류명 · 도서명 · 수량 · 비고
작업일 · 방향 · 작업구분 · 세트 · 세트 수량
```
행 타입(`types.ts`): `recordId, date, kind, warehouse, catName, bookName, qtyDelta, memo, workCost`

### 8p 「주요 데이터 항목」과 대조

| 요구 항목 | 웹 | |
|---|---|---|
| 처리일자 | 입고일/이동일/작업일 | ✅ |
| **거래처** | **없음** | 🔴 |
| 도착/출발창고 | 있음 | ✅ |
| 도서코드/명 | 분류명·도서명 (코드는 없음) | ⚠️ |
| **입고단가** | **없음** | 🔴 |
| 수량 | 있음 | ✅ |
| 이동수량 | 있음 | ✅ |
| 물류작업비여부 | `workCost` (조립 자동계산) | ✅ |

**`입고단가` 누락이 가장 큽니다.** 레거시에 원가가 없어서 순매출·이익률을 못 냈다는 것이
이 프로젝트의 핵심 개선 근거인데(요구사항 15p, `BE-20`), **웹도 입고단가를 안 받고 있습니다.**
지금 구조로는 레거시와 똑같이 원가를 알 수 없습니다.

> 단, 백엔드는 `입고 unit_cost 평균으로 이익률까지 구현 완료`로 문서에 적혀 있습니다
> (`레거시-대비-구현현황.md` 2-2). **백엔드엔 있고 프론트 입력 폼에만 없는** 상태일 수 있습니다.

### 🔴 매입 구분값 미구현 — 2026-07-28 재무팀 확정 요구

요구사항 8p [정정 2026-07-28] 원문:
> ★재무팀 정정 - 신규 필드★ 입고 등록 시 '매입' 콘텐츠임을 구분하는 구분값
> (예: 정상입고/매입입고) 신규 추가 필요. 이 구분값으로 등록된 입고 내역이
> **16p 순매출조회 화면의 매입 데이터로 자동 연결**됨.

웹 `src/features/inbound/` 전체에 `매입` grep **0건** — 미구현입니다.

**개발자도 인지하고 있습니다.** `docs/마일스톤-대조-260731.md:126`:
> **P2-08** 매입 구분값(8p) + 순매출(16p) 연결 — 순매출 화면은 이미 콘텐츠구분 축으로
> 만들어져 있다. **입고 화면의 구분값 필드만 추가하면 연결됨**

→ 작업량이 작고(필드 1개), 연결 대상(16p)은 이미 준비돼 있습니다.

---

## ③ 작동 테스트

| # | 항목 | 결과 |
|---|---|---|
| I1 | 작업모드 3종 | PASS — 탭이 아니라 **한 화면 3개 섹션**(입고 등록 / 단순 이고 / 세트 조립·해체) |
| I2 | 매입 구분값 | **FAIL — 미구현** 🔴 |
| I3 | 입고단가 | **FAIL — 폼에 없음** 🔴 (초기 PASS는 제 탐지 오류) |
| I4 | 창고 선택(물류/위탁) | PASS |
| I5 | 단순이고 — 출발/도착 창고 분리 | PASS |
| I6 | 세트 조립/해체 — 방향·작업구분·세트수량 | PASS |
| — | 콘솔 오류 | **0건** |

> I1·I3 의 최초 판정은 제 셀렉터 오류입니다. 모드 라벨이 `일반입고`가 아니라 `입고 등록`,
> `단순이고`가 아니라 `단순 이고`(공백 포함)였습니다. I3 은 반대로 오탐(PASS)이었고
> 소스 확인 결과 **실제로는 없습니다**.

---

## 📌 문서 불일치 발견

`레거시-대비-구현현황.md` §5-C 「신규 기능(프론트 미착수)」:
> - **매입원가 엑셀 업로드**(외부콘텐츠·이감) 화면 — 백엔드 파서와 함께

이건 **2026-07-28 정정으로 폐기된 범위**입니다. 요구사항 16p 정정 원문:
> 매입 데이터는 이 화면에서 업로드/등록하는 것이 아니라, 8p 입고/대체등록 화면에서
> '매입' 구분값으로 입고 등록된 내역이 자동 연결되는 구조로 정정됨.
> 시스템은 '**자동 업로드 파서**'가 아니라 담당자가 최종 확정한 금액을 입력하는 화면으로 축소

→ 마일스톤 문서(P2-08)는 정정이 반영돼 있는데, **§5-C 향후과제 목록은 정정 전 내용**입니다.
   이 목록을 보고 작업하면 **폐기된 파서를 만들게 됩니다.** 문서 정정 필요.
