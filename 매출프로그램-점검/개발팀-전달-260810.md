# 프론트엔드 점검 결과 전달 — 2026-08-10

작성: 공선재 · 대상 `daesung-free/Dsales_Web` (`develop` `e6dc4fb` 기준)

---

## 0. 먼저

`docs/발주처-회신-260810.md`(오늘 14:34 커밋)를 읽었습니다.
**자료요청서 회신 처리·진행상태 6단계·권한매트릭스 초안·창고 1:1 충돌은 이미 반영/기록돼
있어 이 문서에서 제외**했습니다. 아래는 **그 목록에 없는 것**만입니다.

점검 범위: 메뉴 화면 **32개 전수** + 클라이언트 자료 4종.
방식: **① 레거시 소스 대조 ② 요구사항정의서 대조 ③ 실브라우저 조작·검산** 3축.

결과 요약:

| | |
|---|---|
| 결함 발견 → **수정 완료** | **5건** |
| 결함 — 미수정 | **12건** |
| 결함 0건 화면 | **23개** |
| 계산·양식 검산 | **10개 전부 통과** |
| 요구사항정의서 자체 오류 | **5건** |
| 문서 정정 제안 | **17건** |

검증 5종은 수정 후에도 전부 통과합니다 — 테스트 **332 → 349개**.

---

# ★ 우선순위

## 1순위 — 중도금 판정 항목 (8월 말 검수 대상)

중간검수 기준서 §3.4 기준. **Phase 3 화면군은 판정 대상이 아니므로**
아래 항목이 화면 결함보다 우선합니다.

| # | 항목 | 상태 |
|---|---|---|
| **1-1** | **P2-08 매입 구분값 필드**(8p) + 16p 자동 연결 | ❌ 미구현 (16p 수신 측은 완성) |
| **1-2** | **P2-08 수기대사 보조 화면**(주문내역·판매내역 병렬 비교) | ❌ 미구현 · **어느 추적 문서에도 없음** |
| **1-3** | **S-1 검수 시나리오 — 입고단가** | 판정 기준이 *"원가 기반 원가율·순매출 검산 일치"* 인데 **프론트에 입력란 없음** |

→ **§3 확인 요청 3건** 참조.

## 2순위 — 보안 (실서버 연결 전 결정 필요)

| # | 항목 | 상태 |
|---|---|---|
| **2-1** | **사업자주민번호 마스킹**(`DB-73`) | ❌ 미구현 · **저장소 어디에도 추적 안 됨** |

목업에 값이 비어 있어 지금은 안 보이지만 **실서버 연결 시 그대로 노출**됩니다.
`viewer` 역할도 `masters.view` 가 있어 **전원에게 보입니다**. → **§2-5** 참조.

## 3순위 — 잔금 검수 대상 (Phase 3 화면)

| 순 | 항목 | 근거 |
|---|---|---|
| 3-1 | **수금관리 6건** — 어음 4필드·기장일자·조회기준 3종·일계·수정/삭제 | 요구 10종 중 3종만 구현 |
| 3-2 | **담보비율 컬럼** | 레거시·백엔드 둘 다 있고 프론트만 없음 |
| 3-3 | **정가 컬럼** (출고/반품조회 + 통합매출조회) | 목업에 `bookAmt` 이미 있음. **두 화면 동시 해결** |
| 3-4 | 출고/반품조회 — 구분(상세)·세액·총금액 | 목업에 `tradeKind`·`tax` 있음 |
| 3-5 | **`/sales/bulk` 12열 통일** | 클라이언트 양식 근거 확보 |
| 3-6 | 거래처관리 **만료일** 컬럼 | 「만료된 거래처 포함」 필터의 판정 기준인데 안 보임 |
| 3-7 | 폐기등록 도서별/창고별 토글 · 매출전표 출력 · 매출등록 세액/대분류 | 요구 명시 |

---

# 1. 결함 5건 — 발견 후 수정까지 완료

전부 **화면이 「정상」이라고 알려 주면서 값만 틀린** 종류였습니다.

### 1-1. 매출일괄등록이 폐기된 옛 금액 공식을 쓰고 있었습니다 ★

`src/features/sales/api.ts` `parseBulkText`
```ts
// 기존
Math.round((price * supRate) / 100) * qty   // 단가 반올림 후 곱 — 2026-08-06 확정으로 폐기된 식
// 수정
supplyAmountOf(price, supRate, qty)         // money.ts 정본(버림)
```
**실차이**: 정가 12,345 · 공급률 75% · 수량 150 → `1,388,850` vs `1,388,812` (**38원**)
일괄등록은 수백 건을 한 번에 넣고, 그 매출이 통합매출조회·매출액정리·순매출·계산서신고로 전파됩니다.

### 1-2. 같은 화면이 마스터 대조를 하지 않았습니다
```
존재하지않는거래처 | D.ARCHIVE 국어 | SET | 10 | 75% | 112,500 | 정상 ✓
```
검증이 **빈 문자열 체크뿐**이었습니다. → 마스터 조회 후 없으면 거부.

### 1-3. 정가를 15,000으로 하드코딩
샘플 1행 기준 표시 금액 **1,687,500 → 4,050,000 (2.4배)**.
→ 마스터 정가 조회(입력값 우선). `SAMPLE` 상수도 마스터 실값으로 교체.

> 1-1~1-3 은 `bulkParse.test.ts` **신규 11개**로 고정. 옛 공식과 **다른 값이 나오는지**까지 검사합니다.

### 1-4. `-0` 표시
`shared/grid/format.ts` — `-(returns*12000)` 에서 `returns=0` 이면 JS `-0` → `"-0"` 출력.
→ `nf.format(value + 0)` 정규화.

### 1-5. 매출액 정리 「분류 계」 계층 누락
레거시 `매출액명세서.vb:106~108` 은 **3단 ROLLUP**(소 계 → **분류 계** → 총 계)인데 웹은 2단이었습니다.
요구 15p 도 3단을 명시합니다. → `SubtotalSpec` 에 `outer` 추가. 실화면 **소계 26 + 분류 계 24개** 확인.

**전달 방식**: 브랜치에 있습니다(`e6dc4fb` 기준 리베이스 완료). PR / 패치 중 편한 쪽 알려주세요.

---

# 2. 결함 — 미수정

### 2-1. 🔴 담보비율이 레거시·백엔드에 있는데 프론트에만 없습니다

레거시 `외상매출현황조회.vb:1400~1412` — **담보비율 + 3단계 경고**:

| 조건 | 표시 |
|---|---|
| ≥ 70% | LightPink |
| > 50% | LightGreen |
| **≥ 100%** | 글자 **빨강** |

`shared/api/adapterMap.ts:128` 이 이미 기록해 두셨습니다:
> 백엔드에만 있는 유용한 값: … **`assureRatio`(담보비율)** · `assureLevel`. **화면에 넣을지 검토 대상**

**레거시 근거가 확보됐으니 「검토」→「반영」으로 올릴 수 있습니다.**

> 담보만기 경고도 레거시는 **3단계**(90일/180일/경과)인데 웹은 경과 빨강만 있습니다.
> 1개월 전 알림은 대시보드에 있으니 **그리드 사전 신호만** 없는 상태입니다.
> 발주처 회신(3-2 바)이 *"**해당 화면 진입 시 상단 알림** 및 빨간색 표시"* 를 요구했는데
> **상단 알림이 미구현**입니다.

### 2-2. 🔴 수금관리 — 요구 10종 중 3종

`개발현황-보고-260731.md:95` 에 **「완료」**로 기재돼 있는데 실측이 다릅니다.

레거시 `AmtData` 13컬럼:
```
collDate · collSeq · writeDate · custCode · custName · collKind · collType · collAmt
· prmNum · prmExpiryDate · prmBankName · prmBranchName · memo
```

| 요구 23p | 웹 |
|---|---|
| 수금일자·거래처명·금액·비고 | ✅ |
| **기장일자**(`writeDate`) | ❌ |
| **어음번호·만기일자·은행/지점명** | ❌ |
| 수금구분·입금구분 (2축) | `수단` 1축으로 합침 |

**내부 모순**: `수단` 선택지에 **`어음`이 있는데** 어음번호·만기일자를 넣을 곳이 없습니다.

그 외: **조회대상기준 3종**(레거시 라디오 실측) · **일계** · **수정·삭제**(`api.ts` 에 없음) 미구현.
권한 제어는 정상입니다(`viewer` 로 [수금 등록] 비활성 확인).

`레거시-대비-구현현황.md:71` 은 페이지 번호도 **25p → 23p** 오기입니다.

### 2-3. ⚠️ `/sales/bulk` 6열 양식은 근거가 없습니다

클라이언트 `매출 업로드양식_샘플.xlsx` 실측 — **12열 코드 기반**:
```
거래일자 | 거래처코드 | 학교코드 | 분류코드 | 도서코드 | 회차 | 정가 | 공급률(%) | 수량 | 금액(총금액) | 구분(출고유형) | 메모
샘플: 2026-06-30 | A10A195 | 10002 | S2026A02 | 01 | 4 | 36000 | 0.75 | 10 | 270000 | 정상출고
```
`uploadParse.ts` 와 **컬럼·순서·필수규칙·공급률 소수(0.75)까지 완전 일치**합니다.
양식 경고: *"⚠ 분류코드마다 01부터 재사용되므로 **분류코드 없이는 특정 불가**"*
→ 이름 기반 6열은 동명 상품에 잘못 매칭됩니다. **12열 통일 또는 병행 지원** 제안.

**추가**: 양식은 출고유형 **6종 전부**를 적어뒀는데 `uploadParse.ts` 는 **3종만** 허용합니다.
구현 판단이 맞다고 보지만(위탁·반품·취소는 전용 절차), **양식대로 넣으면 거부되므로**
발주처 회신에 사유를 넣는 편이 좋겠습니다.

### 2-4. 컬럼 누락 (잔금 대상)

| 화면 | 누락 |
|---|---|
| 출고/반품조회(7p) · 통합매출조회(12p) | **정가**·대분류·메모. 목업 `/sales` 에 `bookAmt`·`tax` 가 이미 있어 **컬럼 1줄**. 두 화면이 엔드포인트 공유 |
| 통합매출조회(12p) | **매출전표 출력** — 요구 명시인데 **저장소 문서 어디에도 없음**(grep 0건) |
| 폐기등록(10p) | 도서별/창고별 조회 토글. **폐기 사유도 레거시와 다름** — 레거시는 법인별 3종(`주문착오` 공통), 웹은 단일 5종. **`주문착오` 가 없어** 과거 데이터가 필터에 안 걸립니다 |
| 입고/대체(8p·29p) | **입고단가** — 요구 명시 + 중간검수 S-1 판정 기준 |
| 매출등록(13p) | 세액·총금액·대분류·구분 (위탁 면세 위주라 우선순위는 낮음) |

### 2-5. 🔴 사업자주민번호 마스킹 — 미구현이고 **추적조차 안 됩니다**

요구 30p 기술검증란:
> **사업자주민번호 등 민감정보 포함 — 암호화·마스킹 대상(`DB-73`)**

| | 상태 |
|---|---|
| 레거시 | `bossID as '사업자주민번호'` — **마스킹 없이 출력** |
| 웹 | `{ key: 'ownerRegNo', label: '사업자주민번호' }` — **일반 텍스트 컬럼** |
| 저장소 추적 | **`DB-73`·`마스킹`·`민감정보` grep 0건** |

**다른 미구현들과 성격이 다릅니다** — 반품입고·더프모 스키마 등은 「향후 과제」로 문서에
남아 있지만, `DB-73` 은 아무 데도 없어 **연동 시 그냥 노출**됩니다.

**권한 관점도 필요합니다**: `viewer`·`sales` 도 `masters.view` 가 있어 거래처관리를 봅니다.
사업자주민번호가 **역할 무관 전원 노출** 상태가 됩니다.

→ 최소한 **역할별 노출 제어**(재무·관리자만) 또는 **부분 마스킹**(`######-#******`) 필요.

### 2-6. ⚠️ 거래처관리 — 만료일 컬럼

레거시 26종 중 웹에 없는 것: **등록일 · 만료일 · 모의고사매칭**

**「만료된 거래처 포함」 필터는 구현돼 있는데 `만료일` 컬럼이 없습니다.**
필터는 되는데 **어느 거래처가 언제 만료되는지 화면에서 볼 수 없습니다.**

---

# 3. ★ 확인 요청 3건 — P2-08 (중간검수 항목)

중간검수 기준서 §3.4:
> **P2-08 매입 구분값 필드 + 수기대사 보조**
> 8p 입고/대체등록에 '매입' 구분값 추가, 16p 순매출조회 자동 연결.
> **주문내역·판매내역 병렬 비교 보조 화면**

**현재 상태**:

| | |
|---|---|
| 8p '매입' 구분값 | ❌ (`features/inbound/` 에 `매입` grep 0건) |
| **수기대사 보조 화면** | ❌ (`수기대사`·`병렬`·`대사` grep 0건) |
| 16p 수신 측 | ✅ 완성 (`매입액(B)`·`purchaseAmt`·이익률) |

`마일스톤-대조-260731.md:104,126` 의 *"P2-08 미착수 — **백엔드 선행**"* 은 봤습니다.
그 위에서 세 가지만 확인 부탁드립니다.

1. **정말 백엔드 선행인가요?** 2026-07-28 재무팀 정정이 *"8p 입고 화면에 **구분값 필드
   하나만** 추가"* 로 매우 구체적이고, 기준서 §6.1 「요청 8」도 **부분 수령**입니다.
2. **수기대사 보조 화면이 아직 범위에 있나요?** 마일스톤 표는 「매입 구분값 + 순매출 연결」만
   적고 있고, **보조 화면은 어느 추적 문서에도 없습니다.** 자료요청서(0806)에도 없습니다.
3. **중간검수 시연(S-1)에 P2-08 이 포함되나요?** 판정 기준이 *"원가 기반 원가율·순매출
   산출값이 검산과 일치"* 인데 **프론트에 입고단가 입력란이 없어** 시연 경로가 성립하는지
   함께 봐야 합니다.

---

# 4. 검증 통과 — 중간검수 근거로 쓸 수 있습니다

레거시 소스에서 계산식을 **verbatim 추출해 실데이터로 검산**했습니다. **전부 일치**합니다.

| 화면 | 검증 |
|---|---|
| **제품수불부**(11p) | 재고 방정식·순매출·**취소 30% 분기**·무가 분해 5종. **부호 처리(저장 음수 / 계산 그대로 / 표시 ×−1)까지 대조** |
| **물류 작업비 계산**(28p) | `Σ(자재×단가) + Σ(인원×(BASIC+TRADE))` 일치. 1행 검산 **60,400 = 화면값**. 기타금액을 ETC/LABEL 두 단가로 나눠 곱하는 세부까지 동일 |
| **물류비용등록**(36p) | `LOGCOST` 단가 컬럼(`PAPER·OMR·ETC·LABEL·BASIC·TRADE·PACKTYPE`)이 28p 계산식과 정확히 대응 |
| **월별매출액명세서**(37p) | 소계·합계·부가세 **15항목 전수 검산** |
| **계산서 신고내역**(38p) | 월별 12 + 연간 10 항목. 세액 `공급대가÷11` 정확 |
| **응시현황(연도별)**(17p) | **합계 = 12개월 합** (667 / 8,004,000). 레거시 `bookCnt01~12` 정적 피벗 확인 |
| **거래명세서 출력**(26p) | RDLC 13종 → 서식 1개+파라미터. 총수량 183 검산 일치 |
| **계산서신고**(22p) | **클라이언트 양식 원본 고정부 21종 전부 일치.** 면세 전환 시 세액 컬럼 제거 |
| **매출등록**(13p) | **실조작** — 초과정산 clamp(520→20), **정산+반품이 같은 잔여를 나눠 씀**(10+10=20) |
| **정산내역서** | **end-to-end** — 정산 확정 후 미결 14→13 / 원장 4→5행. **전량 정산 후 원장 유지** 회귀 통과 |
| **학교관리**(35p) | **DSRE2 동기화 6규칙 실동작** — 전체 7→10 · 미사용 0→1 · **전용 1→1 유지** · 거래처 정보 없음 2 |
| **외상매출장/현황**(24·25p) | 잔액 = 이월+매출+교사용+반품−수금 일치 |
| **수익신고**(21p) | 레거시 8컬럼 **순서까지 일치** |
| **작업결과**(27p) | 레거시 18종 중 **16종 순서까지 일치** |
| 로그인·권한 | 21항목 — 계정 열거 방지, 라우트 가드 이중, 역할별 편집 차단 |

**S-5(제품수불부 재고 방정식) 검수 시나리오는 이 대조표를 그대로 쓰실 수 있습니다.**

### 요구사항 기술검증도 실측 확정

`위탁`·`창고`·`BOM`·`이고`·`원가` grep **전부 0건**, `salesData` INSERT **25컬럼**,
파라미터 바인딩 0건, 채번 `seqList.ToArray.Max + 1`, 비원자적 `DELETE`+`INSERT`,
IP 기반 인증(`select userType from IPData where ip='{ip}'`)
— **대조한 10건이 전부 사실**이었습니다.

---

# 5. ★ 요구사항정의서 자체 오류 5건

레거시 원문과 대조한 결과입니다. **매번 웹이 레거시를 따른 쪽이 옳았습니다.**

| # | 위치 | 요구사항 기재 | 레거시 실측 |
|---|---|---|---|
| 5-1 | **sheet2 ⑤ 채번 규칙** | `S+MMDD` / `I+MMDD` / `YYYYMMDD-P0xx` / `OUT-` 4종 | **전부 실체 없음.** `tradeSeq` 는 **정수**이고 (일자×거래분류×거래유형×거래처) 단위 순번. 사용자가 콤보박스에서 고를 수도 있음 |
| 5-2 | **21p 수익신고** | 「총매출금액 / 순매출금액(**비과세·과세**)」 | **`비과세` grep 0건.** 실제는 `거래처코드·거래처명·사업자번호·성명·**월별**·**건수**·순매출금액·**세액**` 8종 |
| 5-3 | **25p 외상매출현황조회** | 데이터 항목에 **담보비율 없음** | 레거시에 담보비율 + **3단계 경고** 실재. `DB-27` 의 *"여신 리스크 관리 강화 시 **신규 설계 필요**"* 도 재검토 대상 — **신규가 아니라 이관** |
| 5-4 | **27p 작업결과** | 「진행상태 신규값: **발송완료/포장중/출력대기**」 | **grep 0건.** 레거시는 상태 컬럼 없이 `출력`·`완료`·`발송일` **날짜 플래그**로 판단. 게다가 2026-08-10 확정 6단계에도 `출력대기`·`포장중` 없음 |
| 5-5 | **36p 물류비용등록** | 「일괄수정은 **완전 신규**(레거시엔 행별 개별수정만)」 | **레거시에 다건 일괄수정 실재**(`물류비용등록.vb:525`). 신규인 것은 **작업구분 혼합 차단**입니다 |

> 5-5 관련: **화면 개선점 배너**의 *"레거시: 행마다 하나씩 개별 수정"* 도 같은 이유로 부정확합니다.
> *"레거시도 일괄수정은 있었으나 작업구분 검증이 없었다"* 가 정확합니다.

**추가 참고 (요구 30p)**: 데이터 항목에 **`만료일`·`등록일` 누락**. 레거시에 실재하고
「만료된 거래처 포함」 필터가 `만료일` 을 씁니다.

---

# 6. 문서 정정 제안

### 6-1. ★ 「향후 과제」 목록이 세 번 낡았습니다

`레거시-대비-구현현황.md` §5-C:

| 항목 | 실제 |
|---|---|
| *"매입원가 엑셀 업로드 화면 — 백엔드 파서와 함께"* | **2026-07-28 정정으로 폐기된 범위** |
| *"위탁 정산내역서(회계기준 회신 대기)"* | **완성됨**(§3 과 모순) |
| *"목표 입력 UI 는 향후 과제"*(:63) | **완성됨.** 실조작 확인 — 12칸 입력 → 저장 → 달성률 `99.8% → 89.5%` 재계산. `개발현황-보고`·`PM-전달-요약` 은 완료로 기재 |

**이 목록을 근거로 작업 계획을 세우면 이미 끝난 것을 다시 만들거나 폐기된 것을 만들게 됩니다.**
목록 전체 재점검을 권합니다.

### 6-2. 나머지

| 문서 | 내용 |
|---|---|
| `CLAUDE.md` §6 | *"lint 경고 12건 **전부** react-refresh"* → 실제 **9건 + `exhaustive-deps` 3건** |
| `레거시-대비-구현현황.md` | *"레거시가 **반품·폐기**를 음수로 저장"* → 실제 **반품·취소·폐기·대체 4종**, **금액 음수화는 반품·취소만** |
| `레거시-대비-구현현황.md`:71 | 수금관리 **25p → 23p** 오기 |
| `개발현황-보고-260731.md`:95 | 수금관리 **「완료」** 재검토 (요구 10종 중 3종) |
| `레거시-대비-구현현황.md` | **매출액정리·순매출조회·응시현황 2종**이 📐골격(추론)인데 실측상 요구/레거시 정확 반영 → **분류 승급 검토** |
| `레거시-대비-구현현황.md` | **출고/반품조회**가 📋"컬럼 확정"인데 **레거시 최소 16컬럼 < 웹 13컬럼** → 분류 재검토 |

> ※ `CLAUDE.md` §5 코드지도의 *"진행상태 4단계 전이"* 는 오늘 6단계 반영과 어긋나 있어
> **제 브랜치에서 함께 고쳤습니다**(같은 커밋에서 놓치신 부분으로 보입니다).

---

# 7. 회수 창구가 없는 항목 2건

| 항목 | 상태 |
|---|---|
| **물류팀 인터뷰** | 요구 26p 가 오픈으로 남긴 ①알람 트리거 조건 ②물류처 전달 문서 범위. **반품입고(29p) 작업 흐름 확인도 여기 묶여 있습니다**(`PM-전달-요약:46`). 그런데 **자료요청서(0806)에 항목이 없습니다** |
| **매출전표 출력**(12p) | 요구 명시인데 **저장소 문서 어디에도 없음** |

---

# 8. 참고 — 브랜치 상태

- 브랜치 `fix/overview-screen-count` (이름이 내용과 안 맞습니다. PR 시 변경 예정)
- **`develop` `e6dc4fb` 기준 리베이스 완료** (진행상태 6단계 반영분 위)
- 검증 5종 통과 · 테스트 **349개** · 콘솔 오류 0
- 문서 동시 갱신 완료 (테스트 수 4곳, `CLAUDE.md` §4.6.19 신설)
- **원격에 푸시하지 않았습니다**

---

# 9. 근거 자료

화면별 상세 문서 30종 + 클라이언트 자료 분석 4종 + 스크린샷 30여 장을 별도 보관 중입니다.
필요하시면 전달드리겠습니다.

| 문서 | 내용 |
|---|---|
| `00~30-*.md` | 화면별 3축 대조 (레거시 원문 인용·검산 수치·실조작 결과) |
| `C1-클라이언트확정사항-반영검증.md` | 발주처 확정 14건 코드 반영 여부 |
| `C2-매출업로드양식-대조.md` | 12열 양식 원본 대조 |
| `C3-중간검수기준서-분석.md` | 중도금 판정 항목과 현 상태 매핑 |
| `C4-자료요청서-회신-분석.md` | 0806 요청서 회신 분석 (※ 귀사 260810 문서와 상당 부분 중복) |

---

# 10. 관찰된 패턴

**① 근거 유무가 품질을 가릅니다**
레거시 소스·RDLC·재무팀 실파일·클라이언트 양식으로 대조한 화면은 **예외 없이 정확**했습니다.
결함 5건은 전부 「근거 없이 추론한 화면」(매출일괄등록)에 몰렸습니다.
해당 화면 코드 주석에도 *"기획서에 상세 양식이 없어 구조를 추론했습니다"* 라고 적혀 있습니다.

**② 계산은 안전, 항목은 누락**
계산식 검산 **10개 전부 통과**. 결함은 **입력·조회 항목이 빠진** 유형이 대부분입니다.

**③ 요구사항정의서보다 레거시가 정확했습니다**
5건의 요구사항 오류에서 **매번 웹이 레거시를 따른 쪽이 옳았습니다.**
20번 응시현황처럼 **스스로 "지어냈다"고 인정하고 레거시로 바로잡은 사례**가 그 방향을 뒷받침합니다.
