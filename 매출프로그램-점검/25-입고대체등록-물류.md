# 25. 물류/작업 > 입고/대체 등록(물류) — 테스트 결과

- 웹: `/logistics/inbound` (8p `/orders/inbound` 와 **같은 컴포넌트, 모드만 제한**)
- 요구사항정의서: **29p** — 「기존수정-경미(권한설정)」
- 레거시: 8p 와 동일 (`UC_TabPages.vb` 입고 탭 → `invenData`)
- 판정: ✅ **요구 29p 핵심 정확히 구현.** 반품입고는 **추적 중인 향후 과제**

---

## ① ★ 이슈 #13·#35 해소 — 한 기능을 두 경로에 노출

요구 29p:
> 이슈 #13, #35 최종 해소: **하나의 백엔드 기능이 메뉴 경로 2곳**(주문/출고관리, 물류/작업)
> **에 노출**되며, 물류/작업 경로에서는 **권한(RBAC)으로 '일반 입고' 모드만 접근** 가능하도록
> 화면 노출 제어 필요

### 실측 — 정확히 그렇게 동작합니다

| 경로 | 노출 모드 |
|---|---|
| `/orders/inbound` (8p) | **입고 등록 · 단순 이고 · 세트 조립/해체** — 3모드 |
| `/logistics/inbound` (29p) | **입고 등록** 1모드만 |

같은 화면 컴포넌트를 쓰면서 **경로에 따라 모드가 달라집니다.**
`logi` 역할로 접근해도 정상 동작합니다(권한 통과 확인).

**요구 29p 의 핵심 요구가 그대로 구현돼 있습니다.**

> L2 가 최초 FAIL 로 나온 것은 제 판정 로직 오류입니다 — 물류 경로 화면 텍스트에
> `단순 이고`·`세트` 가 **없는데도** 제 조건식이 반대로 잡혔습니다.
> 실제 출력(`입고 등록` 섹션 하나 + 입고/대체 내역 그리드)을 보면 **1모드만 노출**됩니다.

---

## ② 🔵 반품 입고 모드 — 미구현, 다만 4개 문서에서 추적 중

요구 29p [업데이트 2026-07-25] ★클라이언트 신규 요청★:
> '일반 입고' 옆에 **'반품 입고' 작업모드 신규 추가** 필요.
> 배경: DSRE 병행 사용 시 물류가 DSRE 에 1차 반품 등록 후 매출프로그램에 동일 수량 복사
> 입력했으나, **매출프로그램 단독 사용 시에는 수불관리가 매출프로그램에서 전부 이뤄져야**
> 하므로 반품 등록의 **원천 자체가 매출프로그램**이 되어야 합니다.
> 물류의 반품 입고 → 주문/매출등록에 반품으로 자동 반영 → 월마감 시 수정.
> **8p 위탁출고 자동이고와 동일하게 트랜잭션 원자성 확보 필요**

**웹 확인 — 미구현**(`입고 등록` 모드만 있음).

### 추적 상태는 탄탄합니다

| 문서 | 기재 |
|---|---|
| `레거시-대비-구현현황.md:82` | *"**반품입고 신규 모드는 클라이언트 요구(260729-②) — 향후 과제** (교재 반품 방식: 기출고 조회→범위 내 차감)"* |
| `레거시-대비-구현현황.md:185` | *"물류 '반품입고' 모드와 매출 반영 **한 트랜잭션 — 백엔드 협의 필요**"* |
| `PM-전달-요약-260729.md:46` | *"동일 방식으로 **설계 예정**이며, **물류팀 인터뷰에서 작업 흐름을 함께 확인**하겠습니다"* (클라이언트 회신 초안) |
| `발주처-회신-260805.md:79` | *"반품입고 결과는 주문/매출등록에 자동 반영, **단일 트랜잭션**"* (발주처 확정) |

→ **결함이 아니라 「백엔드 협의 + 물류팀 인터뷰 대기」 상태의 향후 과제**입니다.
   클라이언트에게 이미 *"설계 예정"* 으로 회신 초안까지 잡혀 있습니다.

### 다만 선행 조건이 막혀 있습니다

`PM-전달-요약` 이 *"물류팀 인터뷰에서 작업 흐름을 함께 확인하겠습니다"* 라고 했는데,
**23번에서 확인했듯 물류팀 인터뷰가 아직 회신 전이고 자료요청서(0806)에도 항목이 없습니다.**

→ **반품입고는 물류팀 인터뷰에 묶여 있고, 그 인터뷰는 회수 창구가 없는 상태**입니다.

---

## ③ 8p 와 공유하는 기존 지적 (03번)

이 화면은 8p 와 **같은 컴포넌트**이므로 03번에서 찾은 항목이 그대로 적용됩니다:

| 항목 | 상태 |
|---|---|
| **입고단가** | ❌ 폼에 없음 — 요구 8p 명시, 중간검수 S-1 판정 기준 |
| **매입 구분값** | ❌ 미구현 — **P2-08 중도금 항목** |
| 거래처 | ❌ 폼에 없음 |

**물류 경로에서도 동일하게 빠져 있습니다.** 특히 **입고단가**는 물류가 실물을 받으면서
입력해야 원가가 잡히는 값이라, 물류 화면에 없는 것이 실무상 더 걸립니다.

---

## ④ 작동 테스트

| # | 항목 | 결과 |
|---|---|---|
| L1 | 입고 등록 섹션 | PASS |
| L2 | **일반입고 1모드만 노출** (요구 29p) | **PASS** |
| L3 | 반품 입고 모드 | 미구현 (추적 중 향후 과제) |
| L4 | 8p 는 3모드 전부 | PASS |
| L5 | `logi` 역할 접근 | PASS |
| — | 입고/대체 내역 그리드 (일자·구분·창고·분류·도서/세트·증감·비고) | PASS |
| — | 콘솔 오류 | **0건** |

---

## 수정 후보

**없습니다** — 반품입고는 근거 있는 대기 항목입니다.

## 확인 대상 (기존 목록 강화)

- **물류팀 인터뷰가 병목입니다.** 23번(알람 조건·전달 문서 범위)과 25번(반품입고 작업 흐름)이
  **모두 이 인터뷰를 기다리고 있는데, 자료요청서(0806)에 항목이 없습니다.**
  → **인터뷰 요청을 자료요청서에 추가하거나 별도 채널로 잡아야 합니다**

---

## 물류/작업 4개 완료

| 화면 | 판정 |
|---|---|
| 작업요청서(26p) | ✅ 결함 0 — 발송구분·등록처·알림 배지 전부 구현 |
| 작업결과(27p) | ✅ 결함 0 — 레거시 16/18 일치. 출고창고 필터는 오늘 접수 요구 |
| **물류 작업비 계산(28p)** | ✅ 결함 0 — **계산식 verbatim + 검산 통과** |
| 입고/대체 등록(물류)(29p) | ✅ 결함 0 — 모드 제한 정확. 반품입고 대기 |

**물류/작업 그룹 4개 전부 무결함**입니다.
