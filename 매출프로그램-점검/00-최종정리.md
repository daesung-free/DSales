# 매출프로그램 웹 — 점검 최종 정리

작성 2026-08-10 · 대상 `daesung-free/Dsales_Web` (`develop` `e6dc4fb` 기준)
**메뉴 화면 32개 전수 점검 완료**

---

## ⚠️ 팀원께 — 개인 문서가 공용 다운로드 폴더에 있습니다

**해당 팀원이 직접 확인하고 정리해 주세요.** 본인 파일이 아니어서 임의로 옮기지 않았습니다.

이 PC 의 `~/Downloads` 에 프로젝트 자료와 개인 문서가 섞여 있습니다.
```
입사지원서(신규).pdf / .docx · 하나은행_입사지원서_서지원.docx
8년 사무경력 + 개발 경험… _jiwontwopunch.pdf · 보험증권.pdf
```
> 파일명만 확인했고 **내용은 열어보지 않았습니다.**

이유: ① 클라이언트 자료를 계속 여는 폴더라 **실수로 함께 첨부될 위험** ② 개인정보 포함
③ 저장소로 잘못 복사되면 **git 이력에 남아 지우기 어려움**

```bash
mkdir -p ~/Documents/개인
mv ~/Downloads/입사지원서* ~/Downloads/하나은행_* \
   ~/Downloads/*jiwontwopunch* ~/Downloads/보험증권.pdf ~/Documents/개인/
```

### 레거시 소스도 같은 주의가 필요합니다

`~/Downloads/DsSales-main` 에는 **평문 DB 계정**이 들어 있을 가능성이 높습니다
(요구사항 Phase 0 순번 2). **절대 `~/Dsales_Web` 안으로 복사하지 마세요.**

---

## 1. 점검 개요

| 항목 | |
|---|---|
| 대상 | 메뉴 화면 **32개 전수** + 클라이언트 자료 4종 |
| 방식 | ① 레거시 소스 대조 ② 요구사항정의서 대조 ③ 실브라우저 조작·검산 |
| 레거시 | `DsSales-main` VB.NET — 폼 64개 / 56,358줄 / RDLC 26개 |
| 결과 | **결함 17건**(수정 5 · 미수정 12) · 결함 0건 화면 **23개** |
| 검산 | 계산·양식 **10개 화면 전부 통과** |
| 부수 | 요구사항 오류 **5건** · 문서 정정 **17건** |

전 화면 **콘솔 오류 0건**. 검증 5종(`typecheck→lint→test→build→audit`) 통과, 테스트 **349개**.

---

## 2. 🔴 결함 5건 — 수정 완료

전부 **화면이 「정상」이라고 알려 주면서 값만 틀린** 종류였습니다.

| # | 결함 | 조치 |
|---|---|---|
| 1 | **매출일괄등록 금액 공식** — 2026-08-06 확정으로 폐기된 「단가 반올림 후 곱」 사용 | `supplyAmountOf()` 정본으로 교체 |
| 2 | **마스터 대조 없음** — `존재하지않는거래처` 가 「정상 ✓」 통과 | 마스터 조회 후 거부 |
| 3 | **정가 하드코딩 15,000** — 표시 금액 2.4배 차이 | 마스터 정가 조회(입력값 우선) |
| 4 | **`-0` 표시** — JS 음수 0 이 `"-0"` 으로 출력 | `format.ts` 정규화 |
| 5 | **매출액정리 「분류 계」 누락** — 레거시 3단 ROLLUP 인데 2단 | `SubtotalSpec.outer` 추가 |

테스트 **17개 추가**(`bulkParse.test.ts` 11 · `subtotals.test.ts` 4 · `format.test.ts` 2).
`CLAUDE.md` **§4.6.19 신설** — 원인·조치·재발 방지 기록.

---

## 3. 🔴 결함 12건 — 미수정

### 우선순위

| 순위 | 성격 | 항목 |
|---|---|---|
| **1** | **중도금 판정**(8월 말) | P2-08 매입 구분값 · 수기대사 보조 화면 · S-1 입고단가 |
| **2** | **보안** | 사업자주민번호 마스킹(`DB-73`) |
| **3** | 잔금 검수 | 수금관리 6건 · 담보비율 · 정가 컬럼 등 |

### 상세

| 화면 | 결함 |
|---|---|
| **거래처관리(30p)** | **사업자주민번호 마스킹 미구현 + 저장소 어디에도 추적 안 됨.** `viewer` 도 `masters.view` 라 전원 노출 · 만료일 컬럼 없음 |
| **수금관리(23p)** | 요구 10종 중 **3종만**. 어음 4필드·기장일자·조회기준 3종·일계·수정/삭제 없음. **`수단`에 `어음`은 있는데 어음번호를 넣을 곳이 없음** |
| **외상매출현황조회(25p)** | **담보비율** — 레거시·백엔드에 있고 프론트만 없음. 만기 사전경고 2단계(90/180일)·진입 시 상단 알림 없음 |
| 출고/반품조회 · 통합매출조회 | **정가**·대분류·메모 · 매출전표 출력 |
| 입고/대체(8p·29p) | **입고단가** · 매입 구분값 · 거래처 |
| 폐기등록(10p) | 도서별/창고별 토글 · 폐기 사유 `주문착오` 누락 |
| `/sales/bulk` | 6열 이름기반 — 클라이언트 양식은 **12열 코드기반** |

---

## 4. ✅ 검증 통과 — 검수 근거로 활용 가능

레거시에서 계산식을 **verbatim 추출해 실데이터로 검산**. **전부 일치**했습니다.

| 화면 | 검증 |
|---|---|
| 제품수불부(11p) | 재고 방정식·순매출·**취소 30% 분기**·무가 분해 · **부호 처리까지** |
| 물류 작업비(28p) | 1행 검산 **60,400 = 화면값** · 기타금액 두 단가 분리까지 동일 |
| 물류비용등록(36p) | `LOGCOST` 단가 컬럼이 28p 계산식과 정확히 대응 |
| 월별매출액명세서(37p) | 소계·합계·부가세 **15항목** |
| 계산서 신고내역(38p) | 월별 12 + 연간 10 · 세액 `공급대가÷11` |
| 응시현황(17p) | **합계 = 12개월 합**(667 / 8,004,000) |
| 거래명세서(26p) | RDLC 13종 → 서식 1개+파라미터 · 총수량 183 |
| 계산서신고(22p) | **클라이언트 양식 21종 전부 일치** |
| 매출등록(13p) | **실조작** — 초과정산 clamp · 정산+반품 잔여 분할 |
| 정산내역서 | **end-to-end** — 미결 14→13 / 원장 4→5 · 전량정산 후 원장 유지 |
| 학교관리(35p) | **DSRE2 동기화 6규칙 실동작** |
| 외상매출장/현황(24·25p) | 잔액 공식 일치 |
| 수익신고(21p) · 작업결과(27p) | 레거시 컬럼 **순서까지 일치** |
| 로그인·권한 | 21항목 |

### 요구사항 기술검증 실측 확정

`위탁`·`창고`·`BOM`·`이고`·`원가` grep **전부 0건** · `salesData` INSERT **25컬럼** ·
파라미터 바인딩 0건 · 채번 `Max+1` · 비원자적 `DELETE`+`INSERT` · IP 기반 인증
— **대조한 10건 전부 사실**.

---

## 5. ★ 요구사항정의서 자체 오류 5건

**매번 웹이 레거시를 따른 쪽이 옳았습니다.**

| # | 요구사항 | 레거시 실측 |
|---|---|---|
| 1 | sheet2 ⑤ 채번 규칙 4종 | **전부 실체 없음.** `tradeSeq` 는 정수 |
| 2 | 21p 「비과세·과세」 | **grep 0건.** 실제는 월별·건수·세액 |
| 3 | 25p 담보비율 누락 | 레거시에 3단계 경고 실재 → **신규 아니라 이관** |
| 4 | 27p 「출력대기/포장중」 | **grep 0건.** 확정 6단계에도 없음 |
| 5 | 36p 일괄수정 「완전 신규」 | **레거시에 실재.** 신규는 **작업구분 혼합 차단** |

추가: 30p 데이터 항목에 **`만료일`·`등록일` 누락**.

---

## 6. 문서 정정 — 「향후 과제」 목록이 세 번 낡았습니다

`레거시-대비-구현현황.md` §5-C:

| 항목 | 실제 |
|---|---|
| 매입원가 엑셀 업로드 화면 | **2026-07-28 정정으로 폐기** |
| 위탁 정산내역서 | **완성됨** |
| 목표 입력 UI | **완성됨** (실조작 확인 — 달성률 재계산까지) |

**이 목록으로 계획을 세우면 끝난 것을 다시 만들거나 폐기된 것을 만듭니다.**

그 외: `CLAUDE.md` lint 문구 · 부호 규칙 기재 · 수금관리 25p→23p 오기 ·
수금관리 「완료」 재검토 · 📐골격 분류 재검토(매출액정리·순매출조회·응시현황) 등 **총 17건**.

---

## 7. 회수 창구가 없는 항목 2건

| 항목 | 상태 |
|---|---|
| **물류팀 인터뷰** | 26p 알람 조건 + 29p 반품입고 흐름의 선행 조건인데 **자료요청서(0806)에 항목 없음** |
| **매출전표 출력**(12p) | 요구 명시인데 **저장소 문서 어디에도 없음** |

---

## 8. 관찰된 패턴

**① 근거 유무가 품질을 가릅니다**
레거시 소스·RDLC·재무팀 실파일·클라이언트 양식으로 대조한 화면은 **예외 없이 정확**.
결함 5건은 전부 **「근거 없이 추론한 화면」**(매출일괄등록)에 몰렸습니다.
해당 코드 주석에도 *"기획서에 상세 양식이 없어 구조를 추론했습니다"* 라고 적혀 있습니다.

**② 계산은 안전, 항목은 누락**
계산식 검산 10개 전부 통과. 결함은 **입력·조회 항목이 빠진** 유형.

**③ 그룹별 편차**

| 그룹 | 결함 |
|---|---|
| 물류/작업 (4) | **0건** |
| 기초관리 (5) | 1건 (마스킹) |
| 주문/출고관리 (5) | 컬럼 누락 위주 |
| 매출관리 (13) | 4건 (전부 수정 완료) |
| **마감관리 (5)** | **2건** — 재무팀 실무 항목 누락 |

---

## 9. 작업 상태

| 항목 | |
|---|---|
| 브랜치 | `fix/bulk-parse-and-subtotal-defects` (`develop` `e6dc4fb` 기준 리베이스 완료) |
| 코드 수정 | 15개 수정 + 1개 신규 · **커밋·PR 완료** |
| 검증 5종 | 전부 통과 · 테스트 **349개** · 콘솔 오류 0 |
| 문서 동시 갱신 | 테스트 수 4곳 · `CLAUDE.md` §4.6.19 신설 |

---

## 10. 문서 목록

| 문서 | 내용 |
|---|---|
| **`개발팀-전달-260810.md`** | **개발팀 전달본** — 우선순위 3단계 + 결함 + 확인 요청 3건 |
| `00-로그인` ~ `30-물류비용등록` (31종) | 화면별 3축 대조 상세 |
| `C1-클라이언트확정사항-반영검증` | 발주처 확정 14건 반영 여부 |
| `C2-매출업로드양식-대조` | 12열 양식 원본 대조 |
| `C3-중간검수기준서-분석` | 중도금 판정 항목 매핑 |
| `C4-자료요청서-회신-분석` | 0806 요청서 회신 분석 |
| `스크린샷/` | 44장 |

### 화면 번호 ↔ 메뉴 대응

| 그룹 | 문서 |
|---|---|
| 인증 | 00 |
| 주문/출고관리 | 01~05 |
| 매출관리 | 06~14, 20, 21 |
| 마감관리 | 16~19, 22 |
| 물류/작업 | 15, 23~25 |
| 기초관리 | 26~30 |
